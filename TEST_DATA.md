# 测试数据（订单与手机号）

本文档列出用于手工测试与演示的订单号与手机号样例。

## 1）外部订单 Mock 范围

模拟的外部订单编号区间如下：

- 订单号区间：`10002300` 到 `10002359`（共 60 条）

可调用接口：

- `GET /api/external/orders/<order_id>`
- `GET /api/external/orders?phone=<手机号>&limit=5`

## 2）示例手机号（Mock 数据）

以下手机号在 mock 数据中固定出现，可用于手机号查询：

- `13800112211`
- `13911112222`
- `13722223333`
- `13633334444`
- `13544445555`

## 3）常用示例指令

- 查询订单：`查一下订单 10002341`
- 催单：`订单 10002341 超时了，帮我催一下`
- 按手机号查：`用手机号 13800112211 查订单`

> 备注：Mock 数据种子固定，订单状态与 ETA 会根据规则变动，查询结果仅用于演示。
